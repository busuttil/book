jQuery(document).ready(function($) {
    var width = $(window).width() -40,
        height = $(window).height() -40;

    $('.container-entry').css({
        width : width,
        height : height
    });
});